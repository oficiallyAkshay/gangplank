# codecov.yml — patch coverage on changed lines is the gate; project
# coverage is informational only (auto = don't fail on drift elsewhere).
coverage:
  status:
    patch:
      default:
        target: 90%
    project:
      default:
        target: auto
comment: false
